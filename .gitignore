node_modules/
target/
.env
# Variantes et sauvegardes de fichiers d'environnement (contiennent des secrets).
# `.env.example` reste versionne : il ne contient que des valeurs factices.
.env.*
!.env.example
.idea/
.vscode/
dist/
*.log
.DS_Store

# Scripts et artefacts de débogage locaux (jamais destinés au dépôt)
Hash.java
TestHash.java
backend/fix_pwd.sql
docker/fix_hashes.sql
temp-bcrypt/

# Exports/logs de données ponctuels (pas du code source)
wc-product-export-*.csv
workers/import_log_*.csv
workers/venv/
__pycache__/
workers/__pycache__/

# Sources de marque du graphiste : 30,7 Mo de fichiers en resolution d'impression.
# Les versionner alourdirait le depot de facon permanente, et chaque clone les
# retelechargerait. Voir frontend/brand/README.md — la decision se revient en
# retirant cette ligne, tant que l'historique ne s'est pas construit dessus.
frontend/brand/originaux/*
# ... sauf les vectoriels : un SVG pese quelques kilo-octets et constitue la source
# de verite du logo. L'exclure reviendrait a perdre le livrable du graphiste.
!frontend/brand/originaux/*.svg
